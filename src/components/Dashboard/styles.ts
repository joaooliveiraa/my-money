import styled from 'styled-components';

export const Container = styled.main` /*main: bom para seções de conteúdo de páginas*/
    max-width: 1120px;
    margin: 0 auto;

    padding: 2.5rem 1rem; /*2.5 rem = cima baixo | 1rem = laterais*/

`;